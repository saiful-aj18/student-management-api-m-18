# Student Management API

A RESTful API built with Node.js, Express.js, MongoDB, and Mongoose to manage student records. It supports CRUD operations, input validation, middleware, and structured JSON responses.
